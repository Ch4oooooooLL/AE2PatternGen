# v1.5

## 更新摘要

- 引入新的持久化配方缓存方式。现在请先在主界面构建或刷新缓存，再使用 `Preview Count` 或 `Generate`。
- `Preview Count` 与 `Generate` 统一改为读取服务端缓存，减少大型配方表的实时扫描开销，并在聊天栏返回缓存进度与统计信息。
- 生成的 Forge 配置文件注释现在同时提供英文与中文说明，便于整合包、服务端和玩家共同维护。
- 如果你发现任何 bug，请到 GitHub Issues 提交反馈。
